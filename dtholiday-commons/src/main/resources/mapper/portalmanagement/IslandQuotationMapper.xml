<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eva.dtholiday.commons.dao.mapper.portalmanagement.IslandQuotationMapper">

    <insert id="insertBatch">
        INSERT INTO dt_island_quotation (island_index_code, quotation_file, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.islandIndexCode}, #{item.quotationFile}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>
</mapper>
